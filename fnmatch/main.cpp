#include <iostream>
#include <fnmatch.h>

int main(int argc, char* argv[])
{
  int ret = -1;

  //std::string pattern = "";
  //std::string fileName = "a";

  //ret = fnmatch(pattern.c_str(), fileName.c_str(), 0);
  //std::cout << "ret = " << ret << std::endl;
  int a;
  (2 > 1 ? a = 2 : a = 1) ;
  
  std::cout << a << std::endl;

  return ret;

}

